spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-client-eureka

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://localhost:7005/eureka/
  instance:
    prefer-ip-address: true